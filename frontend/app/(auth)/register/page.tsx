import { RegisterForm } from '@/features/auth/ui/RegisterForm';

export const metadata = { title: 'Регистрация — Expense Tracker' };

export default function RegisterPage() {
  return <RegisterForm />;
}
